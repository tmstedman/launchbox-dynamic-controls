Emulators — user overrides
===========================

Files here override the shipped emulator data in Defaults/Emulators/.

Subdirectories
--------------
  MAME/          Override the JOYCODE-to-generic-input lookup table.
  RetroArch/     Override per-core controller variant declarations.
