# Core-specific remap (priority 3). RetroArchRemapLoader.Load() reads only the game-specific
# file (priority 1), so this is ignored. If loaded, the B↔C swap here would break the test
# SegaGenesis_MortalKombatII_RetroArchRmpButtonSwap.
input_player1_btn_b = "8"
input_player1_btn_a = "0"
